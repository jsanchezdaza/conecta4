import React from 'react'
import { createRoot } from 'react-dom/client'
import './styles.css'

export const App = () => <div>Conecta4</div>

createRoot(document.getElementById('root')!).render(<App />)
